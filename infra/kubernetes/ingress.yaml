apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meus-apps-backend-ingress
  namespace: meus-apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.meus-apps.oondemand.online
      secretName: app-tls
  rules:
    - host: api.meus-apps.oondemand.online
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: meus-apps-backend-service
                port:
                  number: 80
