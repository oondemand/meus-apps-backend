apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meus-apps-backend-homolog-ingress
  namespace: meus-apps-homolog
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - homolog.api.meus-apps.oondemand.online
      secretName: app-tls
  rules:
    - host: homolog.api.meus-apps.oondemand.online
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: meus-apps-backend-homolog-service
                port:
                  number: 80
