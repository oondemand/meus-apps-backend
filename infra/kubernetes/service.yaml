apiVersion: v1
kind: Service
metadata:
  name: auth-backend-service
  namespace: central-oondemand
spec:
  type: NodePort
  selector:
    app: auth-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
