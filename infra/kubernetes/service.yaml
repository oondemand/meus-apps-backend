apiVersion: v1
kind: Service
metadata:
  name: meus-apps-backend-service
  namespace: central-oondemand
spec:
  type: NodePort
  selector:
    app: meus-apps-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
