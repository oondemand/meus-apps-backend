apiVersion: v1
kind: Service
metadata:
  name: auth-backend-homolog-service
  namespace: central-oondemand-homolog
spec:
  type: NodePort
  selector:
    app: auth-backend-homolog
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
